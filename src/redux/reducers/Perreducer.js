import{INCREMENT,DECREMENT} from '../acyion_types'
let initState = [{name:'kobe'},{name:'wede'}];
export default function operaCount(preState = initState, action) {
  
      return preState;
  
}
